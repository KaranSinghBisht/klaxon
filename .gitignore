node_modules/
dist/
!packages/action/dist/
coverage/
.env
.env.*
!.env.example
*.local
.DS_Store
*.log
# foundry
packages/contracts/out/
packages/contracts/cache/
# never commit these
*.key
klaxon-member*.json
witness-master*

# OMC session state
.omc/
deploy/aws/witness.env

# the landing site builds independently of the pnpm workspace
site/node_modules/
site/.next/
site/.vercel/
site/next-env.d.ts
site/tsconfig.tsbuildinfo

# Working material, not product. demo.md carries the key-ring password and the real
# ntfy topic; planning/ carries specs, agent session state and simulated reviews.
# Both were purged from history before the repo went public — keep them out.
demo.md
planning/

# Local key-ring scratch from `klaxon init` runs against the CLI package. Share A is
# meant to be public, but these are stray working files, not the demo's committed copy.
packages/cli/.klaxon/
packages/cli/klaxon.policy.json
