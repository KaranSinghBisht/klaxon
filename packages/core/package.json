{
  "name": "@klaxon/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsup src/index.ts --format esm --dts --clean", "test": "vitest run --root ../.." },
  "dependencies": {
    "@ledgerhq/ledger-key-ring-protocol": "0.15.2",
    "@ledgerhq/hw-ledger-key-ring-protocol": "0.10.7",
    "zod": "^4.0.0"
  },
  "devDependencies": { "canonicalize": "^4.0.0" }
}
